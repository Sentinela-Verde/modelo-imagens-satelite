# Validado em 2026-08-27 com Python 3.13.15 no Windows 11 (única versão instalada na máquina do
# time nesse momento — rasterio/geopandas/pyproj já têm wheel win_amd64 cp313 prontos, então a
# ressalva original de "usar 3.11 por causa de wheels" não se aplica mais; ver SV-01 no
# docs/plano-execucao.md). Versões travadas em == porque foram testadas juntas nesta combinação
# exata — reprodutibilidade é regra do projeto (CLAUDE.md).
earthengine-api==1.7.41
rasterio==1.5.1
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.1
scikit-learn==1.9.0
joblib==1.5.3
matplotlib==3.11.1
pyyaml==6.0.3
python-dotenv==1.2.3
tqdm==4.70.0
requests==2.34.2
truststore==0.10.4
openpyxl==3.1.5  # exportação de planilhas .xlsx (dados-modelo-impacto/), não usado pelo classificador

# PyTorch é do Plus (Siamese CNN / change detection), NÃO do baseline V1 — decisão D-01/D-02 em
# docs/plano-execucao.md. Instalar separadamente quando SV-22 começar (Colab, não aqui).
