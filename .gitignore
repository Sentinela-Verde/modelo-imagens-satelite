# Ambiente
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Segredos — nunca commitar
.env
*-service-account*.json
*credentials*.json
.config/earthengine/

# Dados e artefatos pesados/gerados — reproduzíveis a partir da fonte
*.tif
*.tiff
*.parquet
data/raw/
data/interim/
data/processed/
models/
outputs/

# Exceções explícitas: estes SÃO versionados mesmo estando sob data/ ou sendo gerados
!data/labels_manual/
!data/manifests/
!.gitkeep

# PNGs de apoio à rotulagem manual (SV-09): ~120MB, regenerados em segundos via
# `python -m sentinela.labeling.candidatos` — pesado demais pra versionar, ao contrário dos
# outros PNGs de reports/figures/ (poucos, leves, e são evidência final, não material de trabalho).
reports/figures/rotulagem/
