# Placeholder — o conteúdo real de config/classes.yml é criado pela tarefa SV-05
# (docs/tarefas/SV-05-taxonomia-classes-remap.md). Este arquivo existe só para marcar o lugar
# e será substituído por config/classes.yml de verdade.
